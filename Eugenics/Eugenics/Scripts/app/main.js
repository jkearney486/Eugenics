(function () {
    "use strict";

    require([
        "knockout",
        "jquery",
        "sprintf",
        "text",
        "knockout-delegatedEvents"
    ],
    function (ko, $, sprintf, text) {
        /*console.log(ko);
        console.log($);
        console.log(sprintf);
        console.log(text);*/
    });
}());
